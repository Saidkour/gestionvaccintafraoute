export const orderSelector = (state) => state.order.orders;
export const limitSelector = (state) => state.order.limit;
export const resultsCountSelector = (state) => state.order.resultsCount;
export const showingFromSelector = (state) => state.order.showingFrom;
export const showingToSelector = (state) => state.order.showingTo;
export const pageSelector = (state) => state.order.page;

export const vaccineSelector = (state) => state.vaccine.vaccines;
